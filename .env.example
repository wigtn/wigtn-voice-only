# =============================================================================
# WIGVO Environment Variables (v2)
# =============================================================================
# .env.example → .env.local 복사 후 실제 값 입력
# 총 8개 변수 (Mock 모드: 5개만 필수, Real 모드: 8개 전부)
# v2: Supabase Client 직접 사용 (Prisma 미사용)
# =============================================================================

# --- Supabase Auth + Database ---
# Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIs...

# --- OpenAI (for natural language parsing) ---
OPENAI_API_KEY=sk-...

# --- ElevenLabs (for AI phone calls) ---
ELEVENLABS_API_KEY=xi-...
ELEVENLABS_AGENT_ID=agent_xxxxxxxxxxxxxxxxxxxx
# Phone Number ID: ElevenLabs 대시보드 → Phone Numbers → 번호 클릭 → URL에서 확인
# 실제 전화 발신 시 필요 (Mock 모드에서는 불필요)
ELEVENLABS_PHONE_NUMBER_ID=phnum_xxxxxxxxxxxxxxxxxxxx

# --- Mock mode ---
# true  = ElevenLabs API 호출 없이 5초 후 자동 완료 (DEFAULT)
# false = 실제 ElevenLabs + Twilio로 전화 발신
ELEVENLABS_MOCK=true

# --- App URL ---
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# --- Test (scripts/test-elevenlabs.mjs 전용) ---
# E.164 형식: +821012345678 (한국 010-1234-5678)
TEST_PHONE_NUMBER=+821012345678
